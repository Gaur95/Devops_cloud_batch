apiVersion: v1
kind: ReplicationController
metadata:
  name: ak-app
spec:
  replicas: 2
  selector:
    drink: coffee
  template:
    metadata:
      name: mypod
      labels:
        drink: coffee
    spec:
      containers:
        - name: myappc
          image: httpd
          ports:
            - containerPort: 80
